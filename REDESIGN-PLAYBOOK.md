# ダッシュボード再構成の手順書（八代市版へ適用するための記録）

2026-08-24に宇城市版（`toritarosan/uki-shien-dashboard`）で実施した全面再構成の記録です。
**八代市版（`toritarosan/yatsushiro-shien-dashboard`）にも同じ変更を行うため**、ユーザーの指示により
手順・判断・検証方法をここに残しています。八代市のセッションでは、まずこのファイルを読んでから作業してください。

このファイルの生URL:
`https://raw.githubusercontent.com/toritarosan/uki-shien-dashboard/main/REDESIGN-PLAYBOOK.md`

参考: 再構成後の宇城市版の実物 → https://toritarosan.github.io/uki-shien-dashboard/
（PR: https://github.com/toritarosan/uki-shien-dashboard/pull/50 に変更内容の要約あり）

---

## 0. 最初に確認すること（八代市版は構造が違う可能性がある）

**このセッションでは八代市リポジトリを直接見ていません。** GitHubアクセスが宇城市リポジトリに
限定されていたためです。したがって以下は「宇城市版で実際に効果があった手順」であり、
八代市版にそのまま当てはまるかは**着手前に自分で確認すること**。

着手前に確認する項目:

1. `index.html` のCSS変数（`--accent` 等）とカード構造が宇城市版と同じか
2. `asof-badge` / `.alert` / `.stats` / `.grid` / `.card` / `.src` のクラス名が同じか
3. **姉妹ページへの固定導線が何か所あるか**（宇城市版は4か所。八代市版は数が違う可能性がある）
   - 削除・上書きは厳禁。八代市版のCLAUDE.mdに固定導線の記載があるはずなので必ず読むこと
4. 八代市版のCLAUDE.mdに書かれた運用ルール（自動デプロイの有無・マージ権限・検証項目）
5. `nav.jump` のセクションid一覧

構造が違っていても、**下の「1. 診断」「3. 書き方のルール」「4. 検証」はそのまま通用する**。
2.のレイアウトだけ八代市版の実情に合わせて調整すること。

---

## 1. 診断 — なぜ読みにくくなるのか

毎日の自動巡回が**既存の文章に追記し続ける**設計だったことが原因。その結果、
各カードが「現在の状態」ではなく「変更履歴」になっていた。

宇城市版で実際に起きていたこと（2026-08-24時点）:

| 箇所 | 症状 |
|---|---|
| `asof-badge`（先頭の時点バッジ） | **1,690文字**の壁のような文章。その日の調査結果が全部書かれていた |
| `.alert`（警告欄） | 局激の8/17〜8/24の再確認履歴、特定非常災害の全措置一覧まで入っていた |
| 統計タイルの `.sub` | 避難者タイルに**8/10以降の全日次数値**が列挙されていた |
| 各カード本文 | 「【8/22新規】」「【8/23再確認】」「【訂正】」が積み重なっていた |
| 「まだ確認できていないこと」 | 1項目ごとに「8/23時点でも」「8/24も再確認したが」という再確認履歴が付いていた |

可視文字数48,659文字。**先頭から読んでも今日どうすればいいのかが分からない状態。**

八代市版も同じ自動巡回で運用されているなら、同じ症状が出ているはず。まず現状を測ること:

```bash
python3 - <<'PY'
import re
h = open('index.html', encoding='utf-8').read()
def visible(x):
    x = re.sub(r'<style.*?</style>|<script.*?</script>|<!--.*?-->', '', x, flags=re.S)
    return re.sub(r'\s+', '', re.sub(r'<[^>]+>', '', x))
print('可視文字数:', f'{len(visible(h)):,}')
m = re.search(r'class="asof-badge">(.*?)</span>', h, re.S)
print('asof-badge:', f'{len(visible(m.group(1))):,}文字' if m else '見つからず')
PY
```

---

## 2. 目標とする構造

上から順に、**「今すぐ知るべきこと」→「調べたいこと」→「経緯」** の順に並べる。

```
ヘッダー
  h1 / 地震の基本情報
  asof-badge … 「情報は YYYY年M月D日（曜）HH:MM 確認」の1行だけ
  top-note  … 「本文は現在の状況だけ。経緯は下部へ」と読者に構造を説明

<div class="today">      ← 新設「今日変わったこと」5項目以内・各項目から該当セクションへリンク
disclaimer               （そのまま）
<div class="alert">      ← 「いま注意すること」4項目以内。身の安全に関わることだけ
nav.jump                 （#kiroku を追加）
guide-banner             ← 姉妹ページへの固定導線。絶対に消さない
em-strip（連絡先）        （そのまま。もともと一目で読める）
統計タイル                ← .sub は2文以内

各セクション（避難所／水・トイレ／…）
  各カード = 現在の状態のみ。追記マーカーなし
  出典（.src）は必ず残す

<section id="mikakunin"> ← 1項目1行
<section id="kiroku">    ← 新設「経緯・訂正の記録」。<details> で折りたたむ
<section id="links">
footer                   ← 「関連ページ:」行は絶対に消さない
```

### 追加するCSS

`.guide-inline` の定義の後あたりに追加（宇城市版と同じ変数名を使っているので、そのまま貼れるはず）:

```css
  /* ---------- today's key points ---------- */
  .today {
    background: var(--surface); border: 2px solid var(--accent);
    border-radius: 8px; padding: 14px 18px; margin: 16px 0; box-shadow: var(--shadow);
  }
  .today h2 { font-size: 15px; margin: 0 0 8px; color: var(--accent); letter-spacing: .04em; }
  .today ul { margin: 0; padding-left: 20px; font-size: 14px; }
  .today li { margin: 5px 0; }
  .today .t-more { font-size: 12.5px; color: var(--muted); margin: 10px 0 0; }

  /* ---------- archive (経緯・訂正の記録) ---------- */
  details.archive {
    background: var(--surface); border: 1px solid var(--line); border-radius: 8px;
    padding: 8px 16px; margin-bottom: 8px;
  }
  details.archive > summary {
    cursor: pointer; font-weight: 700; font-size: 14px; padding: 4px 0; list-style-position: outside;
  }
  details.archive[open] > summary { margin-bottom: 8px; border-bottom: 1px solid var(--line); padding-bottom: 8px; }
  details.archive p { font-size: 13px; margin: 6px 0; }
  details.archive ul { font-size: 13px; padding-left: 20px; margin: 6px 0; }
  details.archive li { margin: 4px 0; }
```

`.alert` にリスト用の指定も足す:

```css
  .alert ul { margin: 6px 0 0; padding-left: 20px; }
  .alert li { margin: 4px 0; }
```

統計タイルで数値が長いとき用（「407世帯651人」など）:

```css
  .stat .value.sm { font-size: 19px; }
```

### 「経緯・訂正の記録」に入れるもの

宇城市版では `<details class="archive">` を8つ作った。八代市版でも同じ切り口が使えるはず:

1. **⚠️ これまでにこのページで行った訂正** — 最優先。訂正は消さずここに残す
2. 激甚災害（本激）・特定非常災害の措置内容 — 制度の詳細説明。alertから移す
3. 局地激甚災害（局激）の指定経緯 — 「見込み」段階の推移
4. 統計値の推移と、出典間の食い違いが解明された経緯
5. 避難所の再編経緯
6. 支援艦艇・支援主体の交代経緯
7. ライフライン復旧の経緯（断水・給水の日付ごとの動き）
8. その他の経緯（支援物資・道路・ボランティア）

---

## 3. 書き方のルール（これを守らないと1週間で元に戻る）

**再構成そのものより、このルールをCLAUDE.mdに明文化することの方が重要。**
宇城市版ではCLAUDE.mdの冒頭近くに「【最重要・2026-08-24 ユーザー指示】」という節を作って書いた。

1. **カード本文には「いま現在の状態」だけを書く。** 「【8/22新規】」「【8/23再確認】」
   「前回は◯◯だったが」のような追記マーカー・経緯の説明を本文に残さない。
   **古い記述は書き足すのではなく置き換える。**
2. **経緯・訂正・過去の推移は `#kiroku` の `<details class="archive">` に移す。**
   訂正は必ずここに残す（黙って直さない方針は維持）。
3. **`asof-badge` は「情報は YYYY年M月D日（曜）HH:MM 確認」の1行だけ。** 調査結果を書かない。
4. **`<div class="today">` を毎回まるごと書き換える。** 実際に変わったことだけを**5項目以内**で、
   各項目1〜2文。該当セクションへの `<a href="#...">` を付ける。
   変化がなかった日は「本日は統計・支援制度に変更はありませんでした」の1行に置き換える
   （**項目を積み残さない**）。
5. **「変化なしを確認した」ことを本文に書かない。** 変化がなければ本文は触らず、
   `asof-badge` とフッターの時刻だけ更新する。確認した事実はCLAUDE.mdの巡回記録に書く。
6. **`.alert` は4項目以内。** 余震・気象・火災・熱中症など、その日に身の安全に関わることだけ。
   制度の解説はここに書かない（該当セクションのカードへ）。
7. **統計タイルの `.sub` は2文以内。** 数値の推移履歴は書かない（`#kiroku` へ）。
8. **「まだ確認できていないこと」は1項目1行。** 再確認履歴を項目内に積まない。

---

## 4. 検証

`tools/verify_structure.py` を八代市リポジトリにもコピーして使う。
**宇城市版・八代市版のどちらでも動くように書いてある**（姉妹ページのリンク数を決め打ちせず、
1つ前のコミットと比べて減っていないかで判定するため）。

```bash
python3 tools/verify_structure.py index.html --asof "2026-08-25 12:00"
git diff --check      # これだけは別途シェルで実行
```

検証する7項目:

| # | 項目 | 失敗したら |
|---|---|---|
| 1 | タグ整合性（`html.parser`） | マージしない |
| 2 | ページ内アンカーが全て `id` と対応 | マージしない |
| 3 | 姉妹ページ導線が減っていないこと | マージしない |
| 4 | `asof-badge`・フッターの時刻が今回の時刻 | マージしない |
| 5 | `asof-badge` が100文字以内（＝1行に保たれている） | マージしない |
| 6 | `today` / `kiroku` セクションの存在 | マージしない |
| 7 | 出典URLの増減（参考表示・自動NGにはしない） | 目視で確認 |

**5番と6番が今回追加した項目**で、これが「また膨らみ始めたら止める」仕掛けになっている。

スクリプトが壊れた入力をちゃんと検出することは確認済み（バッジ膨張・アンカー切れ・
セクション削除・タグ閉じ忘れの4種を同時に入れて、終了コード1で止まることをテストした）。

### 出典URLが落ちていないかの確認（重要）

宇城市版では**17件のURLが落ちていた**。うち9件は「本文に残した事実の出典」だったので復旧した。
検証スクリプトの7番が落ちたURLを列挙するので、**1件ずつ次のどれかに分類すること**:

- ✅ 新しい報に置き換わった（厚労省第46報 → 第57報 など）→ そのままでよい
- ✅ 受付期間が終了した制度を削除した → そのままでよい
- ✅ HTTP 404 になっている → そのままでよい
- ❌ **本文に事実だけ残して出典を落とした** → 復旧すること

---

## 5. 実施手順

1. 八代市版のCLAUDE.md・README.mdを読み、**固定導線の場所と数**・運用ルールを把握する
2. 現状を測る（上の「1. 診断」のスクリプト）
3. `index.html` を作り直す。**事実と出典は1件も捨てず、置き場所を変えるだけ**
4. CSSを追加（上の「2. 目標とする構造」）
5. `tools/verify_structure.py` をコピーして検証。7項目通過を確認
6. 出典URLの増減を1件ずつ分類し、落ちた出典を復旧
7. **CLAUDE.mdに「3. 書き方のルール」8項目を明文化**し、自動マージの前提条件に
   「asof-badgeが100文字以内」「today/kirokuの存在」の2項目を追加
8. README.mdにページ構造の説明と編集方針を追記
9. ブランチを切ってcommit → push → PR作成
10. **マージ前にユーザーに確認する。** 見た目の作り直しは好みが出るところなので、
    宇城市版でも自動マージせずに「マージしますか」と聞き、指示を受けてからマージした

---

## 6. 宇城市版での実績（八代市版の目安）

| | 変更前 | 変更後 |
|---|---|---|
| 可視文字数 | 48,659 | **28,684（59%）** |
| うち下部の折りたたみ | — | 4,496 |
| 最初に目に入る本文 | 48,659 | **24,188（旧版比50%）** |
| `asof-badge` | 1,690文字 | **25文字** |
| 外部出典URL | 171 | 165（落ちた8件はすべて置き換え・受付終了・404） |

八代市版も同程度の圧縮が見込める。ただし**文字数を減らすこと自体が目的ではない**ので、
事実を削って数字を良くしないこと。減るのは重複した経緯の記述であって、事実ではない。

---

## 7. 宇城市版のコミット

- 再構成のコミット: `b2592a2`
- マージコミット: `8993d7c`（PR #50）

差分を見たい場合:

```bash
git show b2592a2 --stat
git diff 0aef23c b2592a2 -- index.html
```
