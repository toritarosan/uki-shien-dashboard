# 災害支援ダッシュボード運用で得られた知見（2026-07-31〜2026-09-13、約25回の自動巡回の記録）

このファイルは、宇城市版ダッシュボード（`toritarosan/uki-shien-dashboard`）を
Claude Codeで約1か月半・25回にわたり自動巡回・更新運用してきた中で得られた、
**再利用可能な一般的知見**をまとめたものです。個別の日付ごとの調査結果は
[`CLAUDE.md`](CLAUDE.md)（内部の引継ぎメモ）に残していますが、そこから
「この種のダッシュボードを別の災害・別の自治体で運用するときにも通用する教訓」
だけを抜き出し、2026-09-13時点で内容を再検証したうえでここに記録します。

姉妹サイト（八代市版）への構造変更の手順は [`REDESIGN-PLAYBOOK.md`](REDESIGN-PLAYBOOK.md)
に別途まとめてあります。このファイルはページ構造ではなく、**日々の情報収集・検証・
公開の運用そのもの**についての知見です。

---

## 1. 表形式PDF・数値データを読むときの必須手順

**最も重要で、最も繰り返し破られた教訓。** 官公庁の被害集計PDFはテキスト抽出すると
表の値が縦一列に流れて出てくるため、目視や「列数を数えただけ」の確認では誤読する。

実際に発生した事故（2026-08-01）: 県資料の「停電戸数（160）」を「住家被害・分類未確定
（160棟）」と読み違え、約12時間公開され続けた。原因は「16列を検算した」と称しながら、
実際には列数を数えただけで各値がどの列に対応するかを突き合わせていなかったこと。

**再発防止の手順（毎回、資料の版が変わるたびにやり直す）:**

1. ヘッダー行から**列名と列数を確定**する。列名は必ずしもヘッダー1行に収まらず、
   表の下のセクション見出し（本件では「停電」「断水」「給水所」）に分かれて
   存在することがあるため、**ヘッダーだけで列数を判断しない**。
2. 各行（本件では市町村）の値を列名へ**機械的にzip対応**させ、辞書化する。
3. **全列について「個別行の合計＝合計行」を検算**する。1列でも不一致なら
   列の対応がずれている証拠であり、検算が通るまで数値を採用しない。
4. 資料の版が変わると**列構成自体が変わる**ことがある（列が増減する、
   順序が変わる）。「前回検算したから今回は省略する」を絶対にしない。
   実際、8/22の巡回で「以前検算したから」と省略を続けた結果、負傷者数の
   欄を8/19時点から3回以上にわたり読み違えたまま公開していたことが発覚した。
5. 検算の過程はスクリプト化し、結果だけでなく**何と何を突き合わせたか**を
   具体的に記録する（「検算した」とだけ書かない）。

この手順を守ってからは、同種の列読み違いは再発していない。

---

## 2. 自動化されたサブエージェント・検索結果を鵜呑みにしない

複数の調査サブエージェントを並行起動する運用では、サブエージェント自身の誤読・
誤URL報告・AI要約のハルシネーションが定常的に発生する。実際に起きた事例:

- 避難所数の食い違いを「10か所」と報告してきたが、閉鎖済み施設を含む表の行数を
  数えていたための誤り（実際は8か所）だった。
- あるサブエージェントが誤ったURL（`soshiki/219/274517.html`）で404を報告したが、
  正しいURL（`soshiki/1/274517.html`）では正常に表示された。
- Web検索の要約が「台風12号が9/11〜12に発生」「本激が9月7日に閣議決定」といった、
  一次資料・既存の確定事実と矛盾する内容を生成することが繰り返し確認された
  （将来日付・別の既往情報との混同とみられる、いわゆるハルシネーション）。

**対処方針:**

- 数値・事実がサブエージェント間で食い違う場合、または既知の確定事実と矛盾する
  場合は、**必ず自分で一次ページ・一次PDFを直接取得して検証する**。
- サブエージェントが報告してきたURLをそのまま信用せず、特にHTTPステータスの
  異常（404等）は自分で元のURLを直接再検証する。
- 検索結果の要約のみに基づく事実は、既存の確定情報と矛盾しないか必ず突き合わせ、
  矛盾があれば採用しない（本文には残さず、必要なら「未確認」として記録する）。

---

## 3. 起動時刻・タイムゾーンは必ず自分で確認する

自動巡回セッションでは、システムプロンプトの参照日付を鵜呑みにすると、
「今日」の認識がずれたまま作業を進めてしまう危険がある。

**教訓:** セッション開始直後に必ず `date -u +%Y-%m-%dT%H:%M:%SZ` を実行し、
JSTへ変換した「実際の今日の日付・曜日」を確定してから、前回までの記録（前回
巡回時点の「今日」の認識）と突き合わせること。特に、当日限定の案内ページ
（フェリーの運航案内など）を扱う場合、確認した時刻が「朝」なのか「夕方」なのか
によって「まだ当日分が表示されているのは当然」なのか「既に翌日分に
切り替わっている」なのかの解釈が変わるため、時刻の桁まで意識する。

---

## 4. 出典（一次情報）特有の癖への対処

同じ情報源でも、ページ内のセクションごとに更新頻度・表記が異なることが
頻繁にあった。代表例:

- **1ページ内に複数の「確認時点」が混在する**（本件では地元紙のライフライン
  情報ページが、給水・断水・入浴・罹災証明などセクションごとに別々の
  タイムスタンプを持っていた）。**1つのセクションの時点を別セクションに
  流用しない**こと。生HTMLでセクションごとのタイムスタンプを直接確認する。
- **ページの「更新表示」と本文中の「集計時点」がずれることがある**
  （更新表示だけ進んで本文の集計時点は前回のまま、というケースが複数回
  発生した）。更新表示だけを見て「進んだ」と判断せず、本文の実際の時点
  表記まで確認する。
- **当日限定の案内が、確認するタイミングによっては既に翌日分に切り替わって
  いて確認できない**ことがある（本件ではフェリーの入浴支援案内が、夕方の
  巡回時点では既に翌日向けに切り替わっていることが繰り返し発生した）。
  「当日確認できなかった」ことと「実施されなかった」ことは別であり、
  断定せず「未確認」と明記する。
- **同一の事実について、複数の出典（自治体・都道府県・報道）が異なる
  数値・異なる時点を採用している**ことがある（避難者数など）。無理に
  1つに統一せず、出典と時点を添えて両論併記し、原因が判明したら記録する。
- **検索結果に、別の災害・別の自治体の情報が混入する**ことがある
  （本件では「令和7年8月豪雨」向けの投稿や、別自治体の受付期間が誤って
  ヒットした事例が複数回あった）。ページの発行主体・対象災害名を必ず
  本文で確認してから採用する。

---

## 5. ねつ造しないための運用ルール

- **「制度が存在すること」と「対象自治体で実際に受付が始まったこと」を
  混同しない。** 県・国レベルで制度が発表されても、市町村側の受付開始は
  別に確認が必要。
- **推測で「見込み」「準備中」と書かない。** 公式に確認できなければ、
  確認日時つきで「未確認」と明記する（「存在しない」という断定でもない）。
- **情報の更新日時・発表日時・制度の受付開始日/期間を区別して記載する。**
  この3つを混同すると、あたかも「発表と同時に受付が始まった」ように
  読めてしまう。
- **外部サービスのIDやURLを推測しない。** リンク先の対象自治体名・対象期間を
  本文まで確認してから掲載する。
- **複数事業者・複数制度の期限を一律化しない。** 携帯電話各社の支援措置など、
  事業者・措置ごとに対象期間が異なる場合はそのまま個別に記載する。
- **誤りは黙って直さない。** 訂正を行った場合は、その旨をページ上（本件では
  折りたたみの「経緯・訂正の記録」セクション）に明示する。誤りを消して
  何もなかったことにしない。

---

## 6. ページ構造: 「今の状態」と「経緯」を分離する

運用開始から1か月弱で、日々の自動更新が**既存の文章に追記し続ける**設計になり、
各カードが「現在の状態」ではなく「変更履歴」に変質してしまった（可視文字数が
48,659文字まで肥大化し、時点バッジだけで1,690文字になった）。

対処として、ページ構造を「今すぐ知るべきこと（today）」→「現在の状態のみを
書くカード本文」→「経緯・訂正の記録（折りたたみ）」の3層に分離し、
**本文には常に「いま現在の状態」だけを書き、経緯は追記ではなく置き換える**
という運用ルールに変更した。詳細な手順とビフォーアフターの実測値は
[`REDESIGN-PLAYBOOK.md`](REDESIGN-PLAYBOOK.md) にまとめている。

このルールを維持するための**機械的な検証**（後述）がないと、数日〜数週間で
再び同じ状態に戻る点に注意。ルールを文書化するだけでは不十分で、検証項目として
組み込むところまでやって初めて維持できた。

---

## 7. 「自動デプロイ」運用の設計と、そのトレードオフ

運用開始当初は「巡回はPR作成まで・人間がマージして公開」だったが、途中から
「巡回が検証を全て通過した場合に限り自分のPRを自分でマージする」自動デプロイ
運用に変更した。

**設計のポイント:**

- **mainへの直接pushは禁止のまま**とし、変更は必ずPR経由にする。これにより、
  誤りが公開された場合も**該当PRをRevertするだけで即座に元の状態へ戻せる**
  （実際に使ったことはないが、この復旧手段があること自体が、検証の
  自動化に踏み切れた理由）。
- 自動マージの前提条件を明文化し、**1つでも欠けたらマージしない**運用にする。
  本件では最終的に次の7項目とした:
  1. HTMLタグの開閉整合性（`html.parser`等での機械検証）
  2. ページ内アンカー（`href="#..."`）が全て対応する`id`を持つこと
  3. 末尾空白等の差分衛生（`git diff --check`）
  4. 固定導線（姉妹ページへのリンク等）が削除されていないこと
  5. ページ内の「情報確認時点」表記が今回の実行時刻に更新されていること
  6. 「情報確認時点」表記が肥大化していないこと（文字数上限のチェック）
  7. 「今日変わったこと」「経緯の記録」など、構造上必須のセクションが
     存在すること
- **これらの検証は一括実行できるスクリプト（`tools/verify_structure.py`）
  として保守する。** 巡回のたびに検証コードを書き直すと、検証自体に
  ばらつきが出て信頼できなくなる。スクリプトが実際に壊れた入力
  （バッジ肥大化・アンカー切れ・固定導線の削除・タグ閉じ忘れ）を
  検出できることを、意図的に壊した入力でテストしてから運用に投入した。
- **失われた安全弁を自覚する。** 自動デプロイ化により「公開前に人間が
  内容を目視する」工程がなくなった。これは、上記1〜7の検証で防げるのは
  あくまで**構造上の欠陥**であり、**内容の事実誤認は検知できない**という
  ことを意味する。事実誤認への対策は検証スクリプトではなく、1.・2.で
  述べた「数値を採用する前の検算」「一次資料の直接確認」の徹底でしか
  埋められない。

---

## 8. 出典リンクの増減は機械的に検査し、1件ずつ分類する

本文を書き換えるたびに、参照している出典URLの本数が意図せず減っていないかを
機械的にカウントする（増減自体は正常なことも多いため、自動でNGにはせず
参考表示にとどめる）。

減っていた場合は、1件ずつ次のいずれかに分類してから確定する:

- 新しい発表・報番号に置き換わった（受付終了・情報更新） → 問題なし
- 対象の制度・支援が終了した → 問題なし
- リンク先がHTTP 404等になった → 問題なし（ただし本文の記述自体は残す）
- **本文に事実だけ残して出典を落としていた** → 復旧が必要

実際に、ページの全面再構成を行った際に17件のURLが落ち、そのうち9件が
「本文に事実は残っているが出典を落としていた」ケースだったため復旧した。
機械的なチェックをかけていなければ気づかなかった規模の欠落だった。

---

## 9. 第三者（GPT等）による敵対的検証は、可能なら使う

自分自身によるセルフチェックは、独立したモデルによる検証より原理的に弱い
（同じ思い込みを引きずったまま見直すことになりやすい）。本運用では、
ローカル環境で別モデル（ChatGPT）による敵対的ファクトチェックをかけられる
仕組みを別途用意し、気になる更新があったときの事後チェックとして使っている。
クラウドの自動巡回環境からは実行できない制約があるため、**自動デプロイの
必須ゲートにはできていない**が、疑わしい変更を見つけたときに人間が
ローカルで検証をかけ、誤りが確認できれば該当PRをRevertまたは修正PRで
是正する、という運用にしている。

---

## まとめ: 一般化できる原則

1. **数値は検算してから使う。検算は毎回やり直す。**（§1）
2. **他エージェント・検索結果の報告は、矛盾や重要な数値ほど自分で裏取りする。**（§2）
3. **「今」を確認してから作業する。** 参照日付・タイムゾーンを思い込みで進めない。（§3）
4. **出典ごとの癖（複数時点・更新遅延・混入）を前提に読む。**（§4）
5. **確認できないことは確認できないと書く。断定と省略を混同しない。**（§5）
6. **「現在の状態」と「経緯」は構造として分離し、追記ではなく置き換えを徹底する。**（§6）
7. **自動化するほど、機械的な検証を先に固める。** ロールバック手段（Revert）を
   用意してから自動化に踏み切る。（§7）
8. **参照整合性（出典・アンカー等）は毎回機械的に検査する。**（§8）
9. **セルフチェックの限界を自覚し、可能なら独立した検証を挟む。**（§9）

これらは災害情報に限らず、AIエージェントが一次資料を継続的に収集・整理して
公開するあらゆる自動化運用に転用できる知見だと考えている。
